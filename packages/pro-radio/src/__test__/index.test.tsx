import React from 'react';
import { render } from '@testing-library/react';
import ProRadio from '../index';

describe('ProRadio', () => {
  it('render content', () => {
    render(<ProRadio />);
  });
});
