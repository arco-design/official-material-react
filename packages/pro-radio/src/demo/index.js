/**
 * @file
 * @name ProRadio
 * @memberOf 数据录入
 * @description 支持更多场景的 Radio 组件。
 * @author yinkaihui
 * @package @arco-materials/pro-radio
 */

/**
 * @name 基本用法
 */
export { default as Basic } from './basic';

/**
 * @name 单选框组
 * @description 通过 `ProRadio.Group` 使用编组单选框。
 */
export { default as Group } from './group';

/**
 * @name 卡片类型
 * @description 设置 `type=card` 即可展示 `card` 类型的单选框
 */
export { default as Card } from './card';

/**
 * @name 卡片单选框组
 * @description 通过 `ProRadio.Group` 并设置 `type=card` 使用卡片类型的编组单选框。
 */
export { default as CardGroup } from './cardGroup';
