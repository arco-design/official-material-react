import React from 'react';
import { render } from '@testing-library/react';
import ResizeTableTitle from '../index';

describe('ResizeTableTitle', () => {
  it('render content', () => {
    render(<ResizeTableTitle />);
  });
});
